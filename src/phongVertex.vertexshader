#version 330 core
layout(location = 0) in vec3 pos;
layout(location = 1) in vec3 n;

out vec4 position;
out vec4 normal;

void main(){
	gl_Position = vec4(pos,1);
	position = vec4(pos,1);
	normal = vec4(n,1);
}