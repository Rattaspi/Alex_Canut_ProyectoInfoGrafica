#version 330 core

in vec2 TexCoord;


// Ouput data
out vec4 color;

uniform sampler2D ourTexture;
uniform sampler2D ourTexture2;
uniform float mCoef;

void main(){
	//color = mix(texture(ourTexture, TexCoord), texture(ourTexture2, TexCoord), mCoef);
	//color = texture(ourTexture, TexCoord);
	color = vec4(1,0,0,1);
}